# Historical C loader provenance and the verified current repair

**The September 16 Qwen loading defect was reproduced, and the repaired constructor has passed exact checkpoint verification. Historical C is not shown to share the defect.** C remains reproducible evidence about the saved candidate ranks and their paired reranker output. Its package versions, loaded tensor state and corpus extraction lineage are not sufficiently recorded to certify a new runtime or explain a model mechanism. The appropriate status is `HISTORICAL_SAVED_RANK_EVIDENCE_WITH_UNVERIFIED_LOADED_STATE_AND_CORPUS_LINEAGE`.

The original complete warning named 625 newly initialized parameters: 310 under `language_model.*` and 315 under `visual.*`. The first structured capture failed independently; **the completed v2 audit then confirmed 625 missing and 625 unexpected keys**, with zero shape mismatches or loading-error messages. This reproduces the checkpoint-prefix mismatch in Sentence Transformers 5.4.0 / Transformers 4.57.6 / Torch 2.8.0; successful construction alone did not establish correct loading. The defective attempt stopped before provider collection. See the [pinned v2 loading record](https://github.com/JimmyK300/Vecna/blob/6222129bc561d66993e30c210ffac45aa9594d14/benchmark-results/astra-retrieval-rd-v1/outputs/fusion/qwen-loading-audit-v2/loading_info.json).

The repaired constructor subsequently verified **all 625 trained tensors, totaling 2,127,532,032 values**, against the exact BF16 checkpoint cast to CPU float32. All four loading-info lists are empty. The [pinned verification report](https://github.com/JimmyK300/Vecna/blob/6222129bc561d66993e30c210ffac45aa9594d14/benchmark-results/astra-retrieval-rd-v1/outputs/fusion/qwen-repair-verification-v1/verification.json) has SHA-256 `8f459c0f25c62a712acea1ef59e4855e0d8d7428dc8623407ec6db9300790eb8`; checkpoint SHA-256 is `c73fa9caeddeb3ff831d46c085a7a5708343248ca777e90f2d486964464509c1`, revision `9f2f7e710d6d81056aa5c0a4f04764fec6bb7bda`. This audit performed no inference or database access. It certifies the identified repaired constructor and checkpoint; a fresh B capture must bind its own constructor proof. It neither supplies historical C's missing runtime authority nor establishes corpus compatibility.

| Evidence surface | Immutable observations | Unresolved authority |
|---|---|---|
| Historical C Qwen export | The pinned September 14 run manifest declares 115 completed queries, Qwen-only top100, `official_l21_l30_all_v2`, and effective DirectML on AMD RX 6900 XT. Its stdout names `Qwen/Qwen3-VL-Embedding-2B`. Archived stderr records 625 distinct parameters materialized under `model.*`: 310 language and 315 visual. No newly initialized-weight warning appears. | No run-bound package versions, inference-loader source hash, full checkpoint hash or loaded-state tensor equality record. |
| Historical C reranker | Archived stderr also records the 625 `model.*` parameters and no newly initialized-weight warning. The comparison binds this stderr by SHA-256 and records DirectML; warning paths identify the separate `qwen3-vl-reranker-amd/.venv`. Saved output retains 20 candidates per nonempty query with verified original-baseline identities. | No exact package versions, run-bound loader implementation, loaded-state hash, or tensor equality proof. The model cache inventory identifies a reranker snapshot/config but does not prove the historical process loaded it. |
| Corpus/index | Historical temporal work reports reading 592 retained `qwen_vl` feature files. The current smoke manifest records 322,924 rows, `qwen_vl`, and `qwen_vl_SCANN` in the named collection. The current extractor source is identified and shares a SentenceTransformer instance for text/image encoding. | No available immutable chain from Qwen feature bytes through the extraction loader/state to an index generation. Collection names, row counts and schema/index descriptions do not fingerprint stored vectors. |

The archived logs are pinned to dataset commit [`1f1ad1baef1e1d31817f6c5a12d4d94133611038`](https://github.com/JimmyK300/official-dataset-control/tree/1f1ad1baef1e1d31817f6c5a12d4d94133611038/evaluation/headless-current-p0-p1-p2-p3-qwen-only-top100-v0). Their Git blob hashes were verified. The three recovered stdout/stderr hashes match the corresponding historical comparison or later host-inventory references exactly. This materially distinguishes the recorded historical loading behavior from the current warning. Materialization messages and absence of a warning are still weaker evidence than comparing loaded tensors with the trained checkpoint. They do not reveal exact library versions. Full artifact identities and the checks are in [sources.json](loader-provenance-evidence/sources.json) and [findings.json](loader-provenance-evidence/findings.json).

Two identity limits remain explicit. The archived run-manifest SHA-256 differs from the host-inventory manifest, including after LF-to-CRLF normalization; its declarations are attributed to the pinned archive. Also, the old comparison names an earlier reranker-output byte hash that remains unreconciled. The current saved reranker JSON has separate host raw/LF/canonical-JSON identity evidence and reproduced scores. Neither discrepancy is evidence that the historical model was randomly initialized.

`inputs/temporal_surface.json` declares Transformers 4.57.1 and Torch 2.4.1+cpu for a **different temporal query experiment**, with `reembedding: false`. Those versions cannot be assigned to C or to corpus extraction. The current Qwen source fingerprint likewise proves the current source, not its historical use. The available provenance implementation supports feature-hash and index-generation claims, but no populated Qwen corpus linkage was found in the inspected local records. That does not establish whether such records exist elsewhere on the host.

C's frozen 113-query counts therefore stay unchanged: baseline R1/R5/R10/R20 = **41/56/65/68**, reranker = **40/61/64/76**; MRR@20 = **0.4243397949 / 0.4320209889**. These are measurements of the archived outputs. The reproduced defect does not invalidate those literal measurements or prove corpus contamination; the verified current repair does not retroactively certify them as correctly loaded historical model runs.

D and E must retain the separate C historical control and B fresh matched Qwen control. Cross-packet rank differences remain descriptive, with different captures and timeline evidence; they are not a matched runtime or fusion-to-reranker intervention. C's operational recommendation is provisional until reproduced with a verified loader and declared corpus compatibility. The smallest provenance step is to recover an existing extraction manifest/index-generation claim and verify selected feature hashes against it, alongside run-bound loader/checkpoint state evidence. If those records are unavailable, record that limit and design a bounded compatibility probe; do not infer the corpus producer from today's loader or begin a full re-embedding merely from this finding.
