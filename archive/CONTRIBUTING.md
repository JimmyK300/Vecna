Issues are tasks
Pull requests are documented changes

When doing a new task, first assign yourself as an assignee (if not already assigned)
Create a new branch with a name similar to the issue
When finished, create a new pull request, merging the current branch into main. The description should contain a link to the original issue, you can link it easily by doing `#1234` <- this will link to issue 1234

