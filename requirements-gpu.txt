# AMD DirectML environment for visual-model acceleration.
# Target: Windows or WSL2 with torch-directml.
#
# This file is intentionally focused on SigLIP/DINO-style PyTorch inference.
# WhisperX and TensorFlow/TransNetV2 remain CPU components for now because
# they use different execution backends.

streamlit>=1.55,<2
protobuf>=6.31.1,<8
numpy>=2.0,<3
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5

# AMD DirectML PyTorch stack.
# Keep these versions together; torch-directml pins the PyTorch ABI.
torch==2.4.1
torchvision==0.19.1
torch-directml==0.2.5.dev240914

# Visual models and CPU-side indexing.
open_clip_torch==3.3.0
transformers==4.57.6
faiss-cpu
opencv-python>=4.8
pillow>=10
ffmpeg-python

# Utilities and app runtime.
requests
loguru
tqdm

# Development
black
isort
